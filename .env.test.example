# Integration tests use a real database.
# Copy to .env.test and adjust if your setup differs.
#
# Docker Compose default:
#   docker-compose up -d
#   npm run test:integration
#
TEST_DATABASE_URL=postgresql://avatar:avatar_password@localhost:5433/avatar_factory
