# ================================================================
# Avatar Factory GPU Worker - UPGRADED Dependencies
# Минорные обновления + новые модели (Whisper, Llama, Video)
# ================================================================

# Core utilities
setuptools>=69.0.0
pip>=24.0
wheel>=0.43.0

# ----------------------------------------------------------------
# FastAPI Server
# ----------------------------------------------------------------
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# ----------------------------------------------------------------
# PyTorch (БЕЗ изменений - критично!)
# ----------------------------------------------------------------
# pip install torch==2.1.0 torchvision==0.16.0 torchaudio==2.1.0 --index-url https://download.pytorch.org/whl/cu118
# torch==2.1.0 - НЕ МЕНЯТЬ!

# ----------------------------------------------------------------
# AI Models - UPGRADED VERSIONS
# ----------------------------------------------------------------
# Минорные обновления для поддержки новых моделей
transformers==4.45.0        # Было: 4.36.2 → Llama 3.3 support (минор)
diffusers==0.30.3           # Было: 0.25.1 → AnimateDiff, SVD improvements (минор)
accelerate==0.26.1          # Было: 0.25.0 → Better memory management (минор)
safetensors>=0.4.3
huggingface-hub==0.20.3

# ----------------------------------------------------------------
# NEW: Whisper STT
# ----------------------------------------------------------------
openai-whisper>=20231117    # Whisper Large V3
# Зависимости Whisper (большинство уже есть)
tiktoken>=0.5.0             # Tokenizer для Whisper

# ----------------------------------------------------------------
# NEW: Video Generation (опционально, для локальных экспериментов)
# ----------------------------------------------------------------
# imageio>=2.34.0           # Video I/O (для AnimateDiff)
# imageio-ffmpeg>=0.5.0     # FFmpeg wrapper

# ----------------------------------------------------------------
# Audio Processing
# ----------------------------------------------------------------
soundfile>=0.12.1
librosa>=0.10.2

# ----------------------------------------------------------------
# Image/Video Processing
# ----------------------------------------------------------------
numpy>=1.26.4,<2.0.0        # БЕЗ ИЗМЕНЕНИЙ - критично для OpenCV!
opencv-python>=4.8.0,<4.10.0  # БЕЗ ИЗМЕНЕНИЙ
pillow>=10.3.0

# ----------------------------------------------------------------
# MuseTalk Dependencies
# ----------------------------------------------------------------
einops>=0.8.0
omegaconf>=2.3.0
ffmpeg-python>=0.2.0
moviepy>=1.0.3
chumpy
mmcv
mmpose
mmdet

# ----------------------------------------------------------------
# NEW: API Clients
# ----------------------------------------------------------------
httpx>=0.27.0               # Для Polza.ai, уже должен быть (FastAPI зависимость)
python-dotenv>=1.0.0        # Для управления .env

# ================================================================
# КРИТИЧНЫЕ ОГРАНИЧЕНИЯ (НЕ МЕНЯТЬ!):
# ================================================================
# - torch==2.1.0 (не обновлять!)
# - numpy<2.0.0 (OpenCV требует 1.x)
# - opencv-python<4.10.0 (4.10+ требует NumPy 2.x)
# ================================================================
