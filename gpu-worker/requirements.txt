# Core utilities
setuptools>=65.0.0
pip>=23.0.0
wheel>=0.40.0

# FastAPI Server
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# PyTorch (install separately with CUDA)
# Versions: torch==2.1.2+cu118, torchvision==0.16.2+cu118, torchaudio==2.1.2+cu118
# pip install torch==2.1.2+cu118 torchvision==0.16.2+cu118 torchaudio==2.1.2+cu118 --index-url https://download.pytorch.org/whl/cu118

# AI Models (compatible with PyTorch 2.1.x)
diffusers==0.25.1
transformers==4.37.2
accelerate==0.26.1
safetensors==0.4.2
huggingface-hub==0.20.3
sentencepiece==0.2.0  # Required by transformers
protobuf==3.20.3  # Stable version

# Audio
soundfile==0.12.1
librosa==0.10.1
pydub==0.25.1

# Image Processing (compatible versions)
opencv-python==4.9.0.80
pillow==10.2.0
imageio==2.33.1
imageio-ffmpeg==0.4.9
scikit-image==0.22.0  # Compatible with numpy 1.26

# Face Enhancement (compatible with torchvision 0.16.x)
# Note: These may still have issues with SadTalker, but work standalone
gfpgan==1.3.8
realesrgan==0.3.0
basicsr==1.4.2

# Utilities (stable versions)
numpy==1.26.3
scipy==1.11.4
tqdm==4.66.1
omegaconf==2.3.0
einops==0.7.0
pyyaml==6.0.1

# Git repos (install after pip install -r requirements.txt)
# SadTalker: git clone https://github.com/OpenTalker/SadTalker.git
# Then: cd SadTalker && pip install -r requirements.txt
