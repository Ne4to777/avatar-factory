# ================================================================
# Avatar Factory GPU Worker - Dependencies
# Optimized for PyTorch 2.7.0 + CUDA 11.8
# ================================================================

# Core utilities
setuptools>=69.0.0
pip>=24.0
wheel>=0.43.0
openmim  # Required for mmcv/mmdet/mmpose installation

# ----------------------------------------------------------------
# FastAPI Server
# ----------------------------------------------------------------
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9  # File uploads

# ----------------------------------------------------------------
# PyTorch (install separately via conda - see Dockerfile)
# ----------------------------------------------------------------
# conda install pytorch==2.7.0 torchvision==0.22.0 torchaudio==2.7.0 pytorch-cuda=11.8

# ----------------------------------------------------------------
# AI Models - Stable Diffusion XL
# ----------------------------------------------------------------
diffusers>=0.30.0        # Stable Diffusion XL pipeline
transformers>=4.41.0     # Required by diffusers
accelerate>=0.30.0       # Memory optimization
safetensors>=0.4.3       # Safe model loading
huggingface-hub>=0.23.0  # Model downloads

# ----------------------------------------------------------------
# Audio Processing - Silero TTS
# ----------------------------------------------------------------
soundfile>=0.12.1  # Audio I/O
librosa>=0.10.2    # Audio processing (MuseTalk)

# ----------------------------------------------------------------
# Image/Video Processing
# ----------------------------------------------------------------
opencv-python>=4.10.0  # Computer vision (MuseTalk)
pillow>=10.3.0         # Image processing
numpy>=1.26.4          # Array operations

# ----------------------------------------------------------------
# MuseTalk Dependencies
# ----------------------------------------------------------------
# Note: MuseTalk cloned separately in Dockerfile
einops>=0.8.0           # Tensor operations
omegaconf>=2.3.0        # Config management
ffmpeg-python>=0.2.0    # Video encoding
moviepy>=1.0.3          # Video manipulation
chumpy                  # Mesh processing (required by mmpose)
mmcv                    # Computer vision (required by MuseTalk)
mmpose                  # Pose estimation (required by MuseTalk)
mmdet                   # Object detection (required by MuseTalk)

# ----------------------------------------------------------------
# Optional: TensorFlow (listed in MuseTalk requirements.txt)
# ----------------------------------------------------------------
# tensorflow>=2.12.0
# NOTE: Not used in our code. MuseTalk uses PyTorch-based Whisper.
# Only include if needed for specific MuseTalk features.

# ================================================================
# REMOVED DEPENDENCIES (not used in code):
# ================================================================
# - gfpgan, realesrgan, basicsr, facexlib (face enhancement - unused)
# - gradio (UI - not needed for server)
# - sentencepiece, protobuf (not needed for our models)
# - pydub, resampy, numba (audio utils - unused)
# - imageio, imageio-ffmpeg, scikit-image (image utils - unused)
# - scipy, tqdm (not directly used)
# - av, gdown (not used)
# ================================================================
