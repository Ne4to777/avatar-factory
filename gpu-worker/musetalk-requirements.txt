# MuseTalk Dependencies (adapted for Avatar Factory GPU Worker)
# Compatible with Python 3.11 + PyTorch 2.7.0

# Core MuseTalk dependencies (already installed in main venv)
# diffusers>=0.30.0
# transformers>=4.40.0
# accelerate>=0.30.0
# huggingface-hub>=0.23.0
# opencv-python>=4.9.0
# soundfile>=0.12.1
# librosa>=0.10.2
# einops>=0.8.0
# omegaconf>=2.3.0
# numpy>=1.26.0
# imageio[ffmpeg]>=2.34.0

# Additional MuseTalk specific packages
ffmpeg-python>=0.2.0
moviepy>=1.0.3
gdown>=5.0.0

# OpenMMLab packages (required by MuseTalk)
# These are installed via openmim in install-musetalk.ps1:
# - openmim (package manager for OpenMMLab)
# - mmcv (computer vision foundation library)
# - mmdet (object detection)
# - mmpose (pose estimation)
# Note: These require C++ compilation and should be installed via mim

# MuseTalk custom models (install from GitHub)
# These need to be installed separately after basic requirements
# git+https://github.com/TMElyralab/MMCM.git@main
# git+https://github.com/TMElyralab/controlnet_aux.git@tme
# git+https://github.com/tencent-ailab/IP-Adapter.git@main
# git+https://github.com/openai/CLIP.git@main

# Note: TensorFlow is NOT required for inference, only for training
# Original MuseTalk requirements include tensorflow==2.12.0 but we skip it
