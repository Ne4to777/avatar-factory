# SadTalker compatible requirements for Python 3.10
# Compatible with main gpu-worker requirements.txt

# Core SadTalker dependencies (no version conflicts)
face-alignment>=1.3.5
kornia>=0.7.0
yacs>=0.1.8

# Audio processing (compatible versions)
librosa>=0.10.0
numba>=0.57.0
resampy>=0.4.2
pydub>=0.25.1

# Optimization
torch-optimizer>=0.3.0

# Image processing (already installed in main venv - will use existing)
# scipy>=1.11.4
# imageio>=2.33.0
# scikit-image==0.22.0
# basicsr==1.4.2
# facexlib>=0.3.0

# YAML and config
pyyaml>=6.0

# Video processing
av>=10.0.0

# Additional SadTalker needs
einops>=0.7.0
tqdm>=4.66.0
